import React from 'react';
import { Box, Typography } from '@mui/material';

const List = () => {
  return (
    <Box sx={{ padding: '20px' }}>
      <Typography variant="h4">List Page</Typography>
      <Typography variant="body1">This is the list page.</Typography>
    </Box>
  );
};

export default List;
